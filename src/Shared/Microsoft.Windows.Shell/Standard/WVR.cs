using System;

namespace Standard;

internal enum WVR
{
    ALIGNTOP = 16,
    ALIGNLEFT = 32,
    ALIGNBOTTOM = 64,
    ALIGNRIGHT = 128,
    HREDRAW = 256,
    VREDRAW = 512,
    VALIDRECTS = 1024,
    REDRAW = 768
}
