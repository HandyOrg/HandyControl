<UserControl x:Class="HandyControlDemo.UserControl.CheckBoxDemoCtl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:langs="clr-namespace:HandyControlDemo.Properties.Langs" hc:DynamicLanguage.ResourceManager="{x:Static langs:Lang.ResourceManager}"
             Background="{DynamicResource RegionBrush}"
             Width="400">
    <hc:TransitioningContentControl>
        <StackPanel Margin="32" HorizontalAlignment="Center" VerticalAlignment="Center">
            <CheckBox Content="{hc:Loc CheckBox}" IsChecked="True"/>
            <CheckBox Margin="0,16,0,0" Content="{hc:Loc CheckBox}" IsChecked="True" IsEnabled="False"/>
            <CheckBox Margin="0,16,0,0" Content="{hc:Loc CheckBox}"/>
            <CheckBox Margin="0,16,0,0" Content="{hc:Loc CheckBox}" IsEnabled="False"/>
            <CheckBox Margin="0,16,0,0" Content="{hc:Loc CheckBox}" IsChecked="{x:Null}"/>
            <CheckBox Margin="0,16,0,0" Content="{hc:Loc CheckBox}" IsChecked="{x:Null}" IsEnabled="False"/>
        </StackPanel>
    </hc:TransitioningContentControl>
</UserControl>
