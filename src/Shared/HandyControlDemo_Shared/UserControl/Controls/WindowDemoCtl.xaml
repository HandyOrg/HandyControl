<UserControl x:Class="HandyControlDemo.UserControl.WindowDemoCtl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:langs="clr-namespace:HandyControlDemo.Properties.Langs" hc:DynamicLanguage.ResourceManager="{x:Static langs:Lang.ResourceManager}"
             xmlns:data="clr-namespace:HandyControlDemo.Data"
             DataContext="{Binding WindowDemo,Source={StaticResource Locator}}"
             Background="{DynamicResource RegionBrush}">
    <hc:TransitioningContentControl>
        <UniformGrid Margin="32" Columns="3" Rows="3" Height="200">
            <Button Command="{Binding OpenWindowCmd}" CommandParameter="{x:Static data:MessageToken.CommonWindow}" Content="{hc:Loc OpenCommonWindow}"/>
            <Button Command="{Binding OpenWindowCmd}" CommandParameter="{x:Static data:MessageToken.BlurWindow}" Content="{hc:Loc OpenBlurWindow}"/>
            <Button Command="{Binding OpenWindowCmd}" CommandParameter="{x:Static data:MessageToken.CustomNonClientAreaWindow}" Content="{hc:Loc OpenCustomNonClientAreaWindow}"/>
            <Button Click="ButtonMessage_OnClick" Content="{hc:Loc OpenMessageWindow}"/>
            <Button Click="ButtonCustomMessage_OnClick" Content="{hc:Loc OpenCustomMessageWindow}"/>
            <Button Click="ButtonCustomContent_OnClick" Content="{hc:Loc OpenCustomContentWindow}"/>
            <Button Name="ButtonMouseFollow" Click="ButtonMouseFollow_OnClick" Content="{hc:Loc OpenMouseFollowWindow}"/>
            <Button Command="{Binding OpenWindowCmd}" CommandParameter="{x:Static data:MessageToken.NoNonClientAreaDragableWindow}" Content="{hc:Loc OpenNoNonClientAreaDragableWindow}"/>
            <hc:Badge Text="New" Style="{StaticResource BadgeDanger}" VerticalAlignment="Center" HorizontalAlignment="Center" BadgeMargin="0,-10,-20,0">
                <Button Command="{Binding OpenWindowCmd}" CommandParameter="{x:Static data:MessageToken.GlowWindow}" Content="{hc:Loc OpenGlowWindow}"/>
            </hc:Badge>
        </UniformGrid>
    </hc:TransitioningContentControl>
</UserControl>
