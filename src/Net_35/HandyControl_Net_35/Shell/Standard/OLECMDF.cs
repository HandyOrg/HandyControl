namespace Standard
{
    using System;

    internal enum OLECMDF
    {
        DEFHIDEONCTXTMENU = 0x20,
        ENABLED = 2,
        INVISIBLE = 0x10,
        LATCHED = 4,
        NINCHED = 8,
        SUPPORTED = 1
    }
}

